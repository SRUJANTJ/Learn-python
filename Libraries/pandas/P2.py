# ML Workflow with Pandas
# Load Data → pd.read_csv()

# Explore → .info(), .describe()

# Clean → Handle missing, duplicates, outliers

# Feature Engineering → Encoding, scaling, new features

# Split Data → train_test_split from scikit-learn

# Feed into Model → model.fit(X_train, y_train)