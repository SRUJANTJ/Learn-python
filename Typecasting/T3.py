# Implicit Import Example
# In some environments, functions like `print` and `len` are available without importing any module
message = "Hello, World!"
length = len(message)  # len() is implicitly available in Python
print(length)  # Output: 13